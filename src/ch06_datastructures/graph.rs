pub struct Graph {}

impl Graph {
    fn new() -> Self {
        Graph {}
    }
}

#[cfg(test)]
mod test {
    use super::*;

    #[test]
    fn test_it_works() {
        let mut _g = Graph::new();
    }

}
